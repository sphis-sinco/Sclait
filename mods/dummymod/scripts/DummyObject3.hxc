class DummyModule3 extends Module
{
	override public function new()
	{
		super('overwriten-dummy-object-3');
	}

	override function onCreate(event:CreateEvent)
	{
		super.onCreate(event);

                trace(event.object);
	}
}
